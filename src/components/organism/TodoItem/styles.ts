import styled from "styled-components";
import { Card } from "../../molecule/Card";

export const TodoItemContainer = styled(Card)`
    flex-direction: row;
    justify-content: space-between;
`;

export const TodoItemDescription = styled.div``;
